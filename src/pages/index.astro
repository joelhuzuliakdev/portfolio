---
import Layout from '../layouts/Layout.astro';

const proyectos = [
	{
		titulo: "Dermalosophy Alviteclat – Web profesional para centro de estética",
		descripcion: "Sitio web desarrollado para marca de estética con presentación de servicios, información de tratamientos y diseño enfocado en la experiencia del usuario. Optimizado para transmitir confianza y facilitar el contacto con clientes potenciales.",
		imagen: "/proyectos/dermalosophy.webp",
		tags: ["Astro", "TailwindCSS", "JavaScript", "Web Corporativa", "Responsive Design", "UI/UX"],
		filtro: "astro",
		enlace: "https://www.dermalosophyalviteclat.com/",
		repo: "https://github.com/joelhuzuliakdev/eclatEspa-a"
	},
	{
		titulo: "Web profesional para estudio de arquitectura",
		descripcion: "Landing page moderna optimizada para captar clientes. Incluye proyectos, servicios y formulario de contacto. Diseñada con Astro y Tailwind.",
		imagen: "/proyectos/arquitectura.webp",
		tags: [
			"Astro",
			"Tailwind",
			"Responsive",
			"Landing Page",
			"SEO optimizado",
			"Alta performance",
			"WhatsApp integrado"
		],
		filtro: "astro",
		enlace: "https://demo-arquitectura-bay.vercel.app/",
		repo: "https://github.com/joelhuzuliakdev/demoArquitectura"
	},
	{
		titulo: "Sistema de turnos para barbería",
		descripcion: "Web profesional con sistema de reservas online y confirmación por WhatsApp.",
		imagen: "/proyectos/barberia.webp",
		tags: [
			"Astro",
			"Tailwind",
			"Responsive",
			"Sistema de turnos",
			"WhatsApp integrado",
			"SEO optimizado"
		],
		filtro: "astro",
		enlace: "https://demo-barberia-eight.vercel.app/",
		repo: "https://github.com/joelhuzuliakdev/demoBarberia"
	},
	{
		titulo: "Sistema de pedidos online con Astro y WhatsApp",
		descripcion: "Desarrollo de una aplicación web utilizando Astro y TailwindCSS que implementa filtrado de productos, gestión de carrito en el frontend y automatización de pedidos mediante integración con :contentReference[oaicite:0]{index=0}.",
		imagen: "/proyectos/hamburgueseria.webp",
		tags: [
			"Astro", 
			"TailwindCSS", 
			"JavaScript", 
			"Carrito de compras", 
			"WhatsApp API", 
			"UI/UX"
		],
		filtro: "astro",
		enlace: "https://bennys-flax.vercel.app/",
		repo: "https://github.com/joelhuzuliakdev/Bennys"
	},
	{
		titulo: "Web empresarial para empresa de packaging",
		descripcion: "Diseño y desarrollo de sitio web moderno para empresa del sector industrial, enfocado en mejorar la presencia digital, comunicar servicios de forma clara y generar nuevas oportunidades de contacto comercial.",
		imagen: "/proyectos/somos_pack.webp",
		tags: [
			"Astro",
			"TailwindCSS",
			"Corporate Website",
			"Business Website",
			"Responsive Design",
			"SEO Friendly"
		],
		filtro: "astro",
		enlace: "https://somos-pack.vercel.app/",
		repo: "https://github.com/joelhuzuliakdev/somosPack"
	},
	{
		titulo: "Web empresarial para empresa de seguridad",
		descripcion: "Diseño y desarrollo de sitio web moderno para empresa de seguridad, enfocado en mejorar la presencia digital, mostrar servicios de vigilancia y protección de manera clara, y generar nuevas oportunidades de contacto comercial.",
		imagen: "/proyectos/seguridad_demo.webp",
		tags: [
			"Astro",
			"TailwindCSS",
			"Corporate Website",
			"Business Website",
			"Responsive Design",
			"SEO Friendly"
		],
		filtro: "astro",
		enlace: "https://demo-seguridad.vercel.app/",
		repo: "https://github.com/joelhuzuliakdev/demoSeguridad"
	}
];

const skills = [
	{ nombre: "React / Next.js", nivel: 90, icono: "⚛️" },
	{ nombre: "TypeScript", nivel: 85, icono: "📘" },
	{ nombre: "CSS / Tailwind", nivel: 95, icono: "🎨" },
	{ nombre: "Astro", nivel: 80, icono: "🚀" },
	{ nombre: "Node.js", nivel: 75, icono: "🌿" },
];
---

<Layout title="Portfolio | Dev Frontend">

	<div id="prog"></div>

	<!-- HEADER -->
	<header>
		<div class="nav-inner">
			<div class="logo">Dev<span>Folio</span></div>
			<nav>
				<a href="#hero">inicio</a>
				<a href="#sobre-mi">sobre_mi</a>
				<a href="#proyectos">proyectos</a>
				<a href="#contacto">contacto</a>
			</nav>
			<div class="nav-right">
				<a href="#contacto" class="btn-contact">Hablemos</a>
				<button class="hamburger" id="ham" aria-label="Menú">
					<span></span><span></span><span></span>
				</button>
			</div>
		</div>
	</header>

	<div class="mobile-nav" id="mobileNav">
		<a href="#hero">inicio</a>
		<a href="#sobre-mi">sobre_mi</a>
		<a href="#proyectos">proyectos</a>
		<a href="#contacto">contacto</a>
	</div>

	<main>
		<!-- HERO -->
		<section id="hero">
			<div class="orb orb-1"></div>
			<div class="orb orb-2"></div>
			<div class="hero-inner">
				<div class="hero-chip">
					<span class="chip-dot"></span>
					disponible_para_proyectos
				</div>
				<h1 class="hero-title">
					Frontend
					<span class="stroke-line">Developer</span>
				</h1>
				<p class="hero-sub">
					Construyo interfaces modernas, rápidas y accesibles.
					Especializado en React, Astro y TypeScript con obsesión por el detalle.
				</p>
				<div class="hero-btns">
					<a href="#proyectos" class="btn-p">
						Ver proyectos
						<svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
					</a>
					<a href="#contacto" class="btn-g">Contactar</a>
				</div>
				<div class="hero-stats">
					<div class="stat"><div class="stat-n">+15</div><div class="stat-l">proyectos</div></div>
					<div class="stat"><div class="stat-n">2</div><div class="stat-l">años_exp</div></div>
					<div class="stat"><div class="stat-n">100%</div><div class="stat-l">satisfacción</div></div>
				</div>
			</div>
		</section>

		<!-- SOBRE MI -->
		<section id="sobre-mi">
			<div class="wrap">
				<div class="reveal">
					<div class="sec-eyebrow">sobre_mi</div>
					<h2 class="sec-title">Quién soy</h2>
					<p class="sec-desc">Desarrollador apasionado por construir productos que la gente ame usar.</p>
				</div>
				<div class="about-grid">
					<div class="about-img-wrap reveal">
						<div class="img-frame">
							<img src="foto-perfil.webp" alt="Mi foto" loading="lazy" />
							<div class="corner c-tl"></div>
							<div class="corner c-br"></div>
						</div>
						<div class="img-badge">
							<div class="badge-icon">⚡</div>
							<div>
								<div class="badge-t">Disponible ahora</div>
								<div class="badge-s">open_to_work: true</div>
							</div>
						</div>
					</div>
					<div class="about-text reveal">
						<p>Hola, soy Joel Huzuliak un desarrollador frontend con más de 2 años creando experiencias digitales que combinan funcionalidad impecable con diseño que destaca.</p>
						<p>Me especializo en el ecosistema de <strong>JavaScript moderno</strong>: React, TypeScript, Astro y Node.js. Trabajo orientado a performance, accesibilidad y código mantenible.</p><p>Trabajé de forma remota para una empresa de España como único desarrollador frontend, siendo responsable del desarrollo completo de las interfaces web, desde la implementación del diseño hasta la optimización de performance y la puesta en producción. Esta experiencia fortaleció mi capacidad de trabajar de forma autónoma, tomar decisiones técnicas y entregar soluciones profesionales.</p>
						<div class="pills">
							<span class="pill">React</span>
							<span class="pill">TypeScript</span>
							<span class="pill">Astro</span>
							<span class="pill">Node.js</span>
							<span class="pill">Tailwind</span>
							<span class="pill">Git</span>
							<span class="pill">Figma</span>
						</div>
						<div class="skill-list">
							{skills.map((s) => (
								<div>
								<div class="skill-head">
									<span class="skill-name">{s.icono} {s.nombre}</span>
									<span class="skill-pct">{s.nivel}%</span>
								</div>
								<div class="skill-track">
									<div class="skill-fill" style={`--w:${s.nivel / 100}`}></div>
								</div>
								</div>
							))}
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- PROYECTOS -->
		<section id="proyectos">
			<div class="wrap">
				<div class="proj-header reveal">
					<div>
						<div class="sec-eyebrow">proyectos</div>
						<h2 class="sec-title">Mis trabajos</h2>
					</div>
					<p class="sec-desc">Soluciones reales a problemas reales.</p>
				</div>
				<div class="filters reveal">
					<!--<button class="filter-btn active" data-f="all">todos</button>
					<button class="filter-btn" data-f="react">react</button>
					<button class="filter-btn" data-f="astro">astro</button>
					<button class="filter-btn" data-f="fullstack">fullstack</button> 
					<button class="filter-btn" data-f="mobile">mobile</button> -->
				</div>
				<div class="proj-grid">
					{proyectos.map((p, i) => (
						<div class="proj-card reveal" data-f={p.filtro} style={`transition-delay:${(i % 3) * 80}ms`}>
							<div class="proj-img-wrap">
								<img class="proj-img" src={p.imagen} alt={p.titulo} loading="lazy" />
								<div class="proj-img-ov"></div>
							</div>
							<div class="proj-body">
								<div class="proj-tags">
									{p.tags.map((t) => <span class="proj-tag">{t}</span>)}
								</div>
								<div class="proj-title">{p.titulo}</div>
								<p class="proj-desc">{p.descripcion}</p>
								<div class="proj-links">
									<a href={p.enlace} class="plink plink-live">
										<svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
										live
									</a>
									<a href={p.repo} class="plink plink-repo">
										<svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
										repo
									</a>
								</div>
							</div>
						</div>
					))}
				</div>
			</div>
		</section>

		<!-- CONTACTO -->
		<section id="contacto">
			<div class="wrap">
				<div class="reveal">
					<div class="sec-eyebrow">contacto</div>
						<h2 class="sec-title">Trabajemos juntos</h2>
						<p class="sec-desc">¿Tenés un proyecto? Escribime y lo hacemos realidad.</p>
					</div>
					<div class="contact-grid">
						<div class="contact-info reveal">
							<p>Estoy disponible para proyectos freelance, colaboraciones y oportunidades laborales. Respondés en menos de 24hs.</p>
							<div class="contact-items">
								<a href="mailto:joelhuzuliakdev@outlook.com" class="contact-item">
									<div class="ci-icon"><svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg></div>
									<div><div class="ci-lbl">email</div><div class="ci-val">joelhuzuliakdev@outlook.com</div></div>
								</a>
								<a href="https://www.linkedin.com/in/joel-huzuliak-837540288/" target="_blank" class="contact-item">
									<div class="ci-icon"><svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg></div>
									<div><div class="ci-lbl">linkedin</div><div class="ci-val">linkedin.com/in/joel-huzuliak-837540288</div></div>
								</a>
								<a href="https://github.com/joelhuzuliakdev" target="_blank" class="contact-item">
									<div class="ci-icon"><svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg></div>
									<div><div class="ci-lbl">github</div><div class="ci-val">github.com/joelhuzuliakdev</div></div>
								</a>
							</div>
						</div>
						<div class="reveal">
							<!-- 👇 Reemplazá XXXXXXXX con tu ID de formspree.io -->
							<form id="cform" action="https://formspree.io/f/xzdaapzz" method="POST" class="cform">
								<div class="form-row">
									<div class="form-group">
										<label for="fn">nombre</label>
										<input type="text" id="fn" name="nombre" placeholder="Tu nombre" required />
									</div>
									<div class="form-group">
										<label for="fe">email</label>
										<input type="email" id="fe" name="email" placeholder="tu@email.com" required />
									</div>
								</div>
								<div class="form-group">
									<label for="fa">asunto</label>
									<input type="text" id="fa" name="asunto" placeholder="¿De qué trata?" required />
								</div>
								<div class="form-group">
									<label for="fm">mensaje</label>
									<textarea id="fm" name="mensaje" placeholder="Contame sobre tu proyecto..." required></textarea>
								</div>
								<button type="submit" class="btn-send">
									Enviar mensaje
									<svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
								</button>
							</form>
							<div class="form-ok" id="formOk">
								<div class="form-ok-icon">✅</div>
								<h3>Mensaje enviado</h3>
								<p>Te respondo en menos de 24hs.</p>
							</div>
						</div>
				</div>
			</div>
		</section>
	</main>

	<!-- FOOTER -->
	<footer>
		<div class="footer-inner">
			<div>
				<div class="footer-logo">Dev<span>Folio</span></div>
				<div class="footer-copy">© <span id="yr"></span> — hecho con Astro ✦</div>
			</div>
			<div class="socials">
				<a href="https://github.com/joelhuzuliakdev" target="_blank" class="social" aria-label="GitHub">
					<svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
				</a>
				<a href="https://www.linkedin.com/in/joel-huzuliak-837540288/" target="_blank" class="social" aria-label="LinkedIn">
					<svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
				</a>
				<a href="https://wa.me/5493572548014" class="social" aria-label="WhatsApp" target="_blank" rel="noopener noreferrer">
					<svg fill="currentColor" viewBox="0 0 24 24">
						<path d="M20.52 3.48A11.8 11.8 0 0 0 12.05 0C5.54 0 .24 5.3.24 11.81c0 2.08.54 4.12 1.57 5.93L0 24l6.46-1.69a11.8 11.8 0 0 0 5.59 1.43h.01c6.51 0 11.81-5.3 11.81-11.81 0-3.16-1.23-6.13-3.35-8.45zM12.06 21.5c-1.78 0-3.52-.48-5.05-1.38l-.36-.21-3.83 1 1.02-3.73-.24-.38a9.44 9.44 0 0 1-1.46-5c0-5.22 4.24-9.47 9.47-9.47 2.53 0 4.91.99 6.7 2.78a9.4 9.4 0 0 1 2.77 6.69c0 5.23-4.25 9.47-9.48 9.47zm5.2-7.1c-.28-.14-1.64-.81-1.9-.9-.25-.1-.43-.14-.62.14-.18.28-.71.9-.87 1.08-.16.18-.32.21-.6.07-.28-.14-1.17-.43-2.22-1.37-.82-.73-1.37-1.64-1.53-1.92-.16-.28-.02-.43.12-.57.13-.13.28-.32.43-.48.14-.16.18-.28.28-.46.1-.18.05-.35-.02-.49-.07-.14-.62-1.5-.85-2.06-.22-.53-.45-.46-.62-.47l-.53-.01c-.18 0-.47.07-.71.35-.25.28-.95.93-.95 2.27s.97 2.63 1.1 2.81c.14.18 1.9 2.9 4.6 4.07.64.28 1.14.45 1.53.58.64.2 1.23.17 1.7.1.52-.08 1.64-.67 1.87-1.32.23-.66.23-1.22.16-1.33-.07-.11-.25-.18-.53-.32z"/>
					</svg>
				</a>
			</div>
		</div>
	</footer>

	<!-- Fonts -->
	<link rel="preconnect" href="https://fonts.googleapis.com" />
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
	<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Outfit:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />

	<style is:global>
		:root {
			--bg:       #080b10;
			--bg2:      #0d1117;
			--bg3:      #111827;
			--surface:  #161d2a;
			--surface2: #1c2537;
			--border:   rgba(255,255,255,0.07);
			--border2:  rgba(99,179,237,0.25);
			--text:     #e8edf5;
			--muted:    #6b7a96;
			--accent:   #63b3ed;
			--accent2:  #4fd1c5;
			--glow:     rgba(99,179,237,0.12);
			--radius:   12px;
			--t:        0.25s cubic-bezier(0.4,0,0.2,1);
		}
		*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
		html { scroll-behavior: smooth; }
		body {
			font-family: 'Outfit', sans-serif;
			background: var(--bg);
			color: var(--text);
			line-height: 1.7;
			overflow-x: hidden;
		}
		h1,h2,h3 { line-height: 1.1; }
		a { color: inherit; text-decoration: none; }
		img { display: block; max-width: 100%; }
		::-webkit-scrollbar { width: 4px; }
		::-webkit-scrollbar-track { background: var(--bg); }
		::-webkit-scrollbar-thumb { background: var(--accent); border-radius: 2px; }
	</style>

	<style>
		/* SCROLL PROGRESS */
		#prog {
			position: fixed; top: 0; left: 0; height: 2px; width: 0;
			background: linear-gradient(90deg, var(--accent), var(--accent2));
			z-index: 1000; transition: width 0.08s linear;
		}

		/* NOISE */
		body::before {
			content: '';
			position: fixed; inset: 0; z-index: 0; pointer-events: none;
			background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.03'/%3E%3C/svg%3E");
			opacity: 0.35;
		}

		/* HEADER */
		header {
			position: fixed; top: 0; left: 0; right: 0; z-index: 100;
			background: rgba(8,11,16,0.88);
			backdrop-filter: blur(16px);
			-webkit-backdrop-filter: blur(16px);
			border-bottom: 1px solid var(--border);
		}
		.nav-inner {
			max-width: 1100px; margin: 0 auto; padding: 0 2rem;
			height: 64px; display: flex; align-items: center; justify-content: space-between;
		}
		.logo {
			font-family: 'Bebas Neue', sans-serif;
			font-size: 1.6rem; letter-spacing: 3px; color: var(--accent);
		}
		.logo span { color: var(--text); }
		nav { display: flex; gap: 0.2rem; }
		nav a {
			padding: 0.35rem 0.9rem; border-radius: 8px;
			font-family: 'JetBrains Mono', monospace;
			font-size: 0.82rem; color: var(--muted);
			transition: color var(--t), background var(--t);
		}
		nav a:hover { color: var(--accent); background: var(--glow); }
		.nav-right { display: flex; align-items: center; gap: 0.8rem; }
		.btn-contact {
			padding: 0.42rem 1.1rem; border: 1px solid var(--border2);
			border-radius: 8px; font-size: 0.84rem; font-weight: 600;
			color: var(--accent); background: var(--glow); transition: all var(--t);
		}
		.btn-contact:hover { 
			background: var(--accent);
			color: var(--bg);
			box-shadow: 0 0 20px var(--glow); 
		}

		.hamburger {
			display: none; 
			flex-direction: column; gap: 5px; 
			cursor: pointer;
			padding: 6px; 
			border-radius: 8px; 
			border: 1px solid var(--border); 
			background: var(--surface);
		}
		.hamburger span { 
			width: 20px;
			height: 2px; 
			background: var(--text); 
			border-radius: 2px; transition: all var(--t); 
			display: block; 
		}
		.hamburger.open span:nth-child(1) { 
			transform: rotate(45deg) translate(5px,5px); 
		}
		.hamburger.open span:nth-child(2) { 
			opacity: 0; 
		}
		.hamburger.open span:nth-child(3) { 
			transform: rotate(-45deg) translate(5px,-5px); 
		}

		.mobile-nav {
			display: none; 
			position: fixed; 
			top: 64px; 
			left: 0; 
			right: 0; 
			z-index: 99;
			background: rgba(8,11,16,0.97); 
			border-bottom: 1px solid var(--border);
			padding: 1rem 2rem 1.5rem; 
			flex-direction: column; 
			gap: 0.3rem;
		}
		.mobile-nav.open { 
			display: flex; 
		}
		.mobile-nav a {
			padding: 0.75rem 1rem; 
			border-radius: 8px; color: var(--text);
			font-family: 'JetBrains Mono', monospace; font-size: 0.95rem;
			transition: background var(--t), color var(--t);
		}
		.mobile-nav a:hover { 
			background: var(--surface); 
			color: var(--accent); 
		}

		/* HERO */
		#hero {
			min-height: 100vh; 
			display: flex; 
			align-items: center; 
			justify-content: center;
			position: relative; 
			overflow: hidden; 
			padding: 8rem 2rem 4rem; 
			text-align: center;
		}
		.orb {
			position: absolute; 
			border-radius: 50%; 
			filter: blur(80px); 
			pointer-events: none;
		}
		.orb-1 { 
			width: 500px;
			height: 500px;
			background: radial-gradient(circle, rgba(99,179,237,0.1), transparent 70%); 
			top: -100px; 
			left: -150px; 
		}
		.orb-2 { 
			width: 400px; 
			height: 400px; 
			background: radial-gradient(circle, rgba(79,209,197,0.08), transparent 70%); 
			bottom: 0; 
			right: -100px; 
		}
		#hero::before {
			content: ''; 
			position: absolute; 
			inset: 0;
			background-image: linear-gradient(var(--border) 1px, transparent 1px), linear-gradient(90deg, var(--border) 1px, transparent 1px);
			background-size: 60px 60px;
			mask-image: radial-gradient(ellipse 70% 70% at 50% 50%, black, transparent);
		}
		.hero-inner { 
			position: relative; 
			z-index: 1; 
			max-width: 900px; 
		}

		.hero-chip {
			display: inline-flex; 
			align-items: center; 
			gap: 0.6rem;
			font-family: 'JetBrains Mono', monospace; 
			font-size: 0.78rem; 
			color: var(--accent);
			border: 1px solid var(--border2); 
			background: var(--glow);
			padding: 0.35rem 1rem; 
			border-radius: 999px; 
			margin-bottom: 2rem;
			animation: fadeUp 0.6s both;
		}
		.chip-dot { 
			width: 6px; 
			height: 6px; 
			border-radius: 50%; background: var(--accent2); 
			animation: blink 1.5s infinite; 
		}

		@keyframes blink { 
			0%,100%{opacity:1} 50%{opacity:0.2} 
		}

		h1.hero-title {
			font-family: 'Bebas Neue', sans-serif;
			font-size: clamp(4.5rem, 13vw, 10rem);
			letter-spacing: 4px; line-height: 0.92; margin-bottom: 1.5rem;
			animation: fadeUp 0.7s 0.1s both;
		}
		.stroke-line {
			display: block;
			-webkit-text-fill-color: transparent;
			-webkit-text-stroke: 1.5px var(--accent);
			opacity: 0.65;
		}
		.hero-sub {
			font-size: 1.1rem; 
			color: var(--muted); 
			max-width: 500px;
			margin: 0 auto 2.5rem; 
			animation: fadeUp 0.7s 0.2s both;
		}
		.hero-btns {
			display: flex; 
			gap: 1rem; 
			justify-content: center; 
			flex-wrap: wrap;
			animation: fadeUp 0.7s 0.3s both;
		}
		.btn-p {
			padding: 0.85rem 2rem; 
			background: var(--accent); 
			color: var(--bg);
			border-radius: 10px; 
			font-weight: 700; 
			font-size: 0.95rem;
			display: inline-flex; 
			align-items: center; 
			gap: 0.5rem; 
			transition: all var(--t);
		}
		.btn-p:hover { 
			transform: translateY(-2px); 
			box-shadow: 0 8px 30px rgba(99,179,237,0.3); 
		}
		.btn-g {
			padding: 0.85rem 2rem; 
			background: transparent; 
			color: var(--text);
			border-radius: 10px; 
			font-weight: 600; 
			font-size: 0.95rem;
			border: 1px solid var(--border); 
			display: inline-flex; 
			align-items: center; 
			gap: 0.5rem;
			transition: all var(--t);
		}
		.btn-g:hover { 
			border-color: var(--accent);
			color: var(--accent); 
			transform: translateY(-2px); 
		}

		.hero-stats {
			display: flex; 
			justify-content: center; 
			gap: 4rem; 
			margin-top: 5rem;
			animation: fadeUp 0.7s 0.4s both;
		}
		.stat { 
			text-align: center; 
		}
		.stat-n {
			font-family: 'Bebas Neue', sans-serif; 
			font-size: 3rem; 
			letter-spacing: 2px;
			color: var(--accent); 
			line-height: 1; 
			text-shadow: 0 0 30px rgba(99,179,237,0.3);
		}
		.stat-l { 
			font-size: 0.75rem; 
			color: var(--muted); 
			margin-top: 0.3rem; 
			font-family: 'JetBrains Mono', monospace; 
		}

		/* SECTION BASE */
		section { 
			padding: 7rem 2rem; 
			position: relative; 
			z-index: 1; 
		}
		.wrap { 
			max-width: 1100px; 
			margin: 0 auto; 
		}

		.sec-eyebrow {
			font-family: 'JetBrains Mono', monospace; 
			font-size: 0.73rem;
			letter-spacing: 0.15em; 
			color: var(--accent); 
			text-transform: uppercase;
			display: flex; 
			align-items: center; 
			gap: 0.5rem; 
			margin-bottom: 0.7rem;
		}
		.sec-eyebrow::before { 
			content: '//'; 
			color: var(--accent2); 
			font-weight: 700; 
		}
		.sec-title {
			font-family: 'Bebas Neue', sans-serif;
			font-size: clamp(2.5rem, 5vw, 4rem); 
			letter-spacing: 2px; 
			margin-bottom: 0.8rem;
		}
		.sec-desc { 
			color: var(--muted); 
			max-width: 460px; 
			font-size: 1rem; 
		}

		/* SOBRE MI */
		#sobre-mi { 
			background: var(--bg2); 
		}
		.about-grid {
			display: grid; 
			grid-template-columns: 1fr 1.4fr;
			gap: 5rem; 
			align-items: center; 
			margin-top: 4rem;
		}
		.about-img-wrap { 
			position: relative; 
		}
		.img-frame {
			position: relative; 
			border-radius: 16px; 
			overflow: hidden;
			border: 1px solid var(--border);
		}
		.img-frame img { 
			width: 100%; 
			aspect-ratio: 3/4; 
			object-fit: cover; 
			filter: brightness(0.9) saturate(0.85); 
		}
		.img-frame::after {
			content: ''; 
			position: absolute; 
			inset: 0;
			background: linear-gradient(to top, rgba(8,11,16,0.55), transparent 50%);
		}
		.corner { 
			position: absolute; 
			width: 22px; 
			height: 22px; 
			border-color: var(--accent); 
			border-style: solid; 
			z-index: 2; 
		}
		.c-tl { 
			top: -8px; 
			left: -8px; 
			border-width: 2px 0 0 2px; 
		}
		.c-br { 
			bottom: -8px; 
			right: -8px; 
			border-width: 0 2px 2px 0; 
		}
		.img-badge {
			position: absolute; 
			bottom: 1.5rem; 
			left: 50%; 
			transform: translateX(-50%); 
			z-index: 3;
			background: var(--surface); 
			border: 1px solid var(--border2); 
			border-radius: 12px;
			padding: 0.8rem 1.2rem; 
			display: flex; 
			align-items: center; 
			gap: 0.8rem;
			white-space: nowrap; 
			box-shadow: 0 8px 32px rgba(0,0,0,0.4);
		}
		.badge-icon { 
			font-size: 1.4rem; 
		}
		.badge-t { 
			font-weight: 700; 
			font-size: 0.88rem; 
		}
		.badge-s { 
			font-size: 0.72rem; 
			color: var(--muted); 
			font-family: 'JetBrains Mono', monospace; 
		}

		.about-text > p { 
			color: var(--muted); 
			font-size: 1.02rem; 
			margin-bottom: 1rem; 
			line-height: 1.85; 
		}
		.pills { 
			display: flex; 
			flex-wrap: wrap; 
			gap: 0.5rem; 
			margin: 1.8rem 0; 
		}
		.pill {
			padding: 0.38rem 0.85rem; 
			border-radius: 7px; 
			border: 1px solid var(--border);
			background: var(--surface); 
			font-family: 'JetBrains Mono', monospace;
			font-size: 0.78rem; 
			color: var(--text); 
			transition: all var(--t);
		}
		.pill:hover { 
			border-color: var(--accent); 
			color: var(--accent); 
			background: var(--glow); 
		}

		.skill-list { 
			display: flex; 
			flex-direction: column; 
			gap: 0.9rem; 
		}
		.skill-head { display: flex; 
			justify-content: space-between; 
			margin-bottom: 0.35rem; 
		}
		.skill-name { 
			font-size: 0.88rem; 
			font-weight: 500; 
		}
		.skill-pct { 
			font-family: 'JetBrains Mono', monospace;
			font-size: 0.78rem; 
			color: var(--accent); 
		}
		.skill-track { height: 4px; 
			background: var(--surface2); 
			border-radius: 999px; 
			overflow: hidden; 
		}
		.skill-fill {
			height: 100%; 
			border-radius: 999px;
			background: linear-gradient(90deg, var(--accent), var(--accent2));
			transform: scaleX(0); 
			transform-origin: left;
			transition: transform 1.1s cubic-bezier(0.4,0,0.2,1);
		}
		.skill-fill.on { 
			transform: scaleX(1); 
		}

		/* PROYECTOS */
		#proyectos { 
			background: var(--bg); 
		}
		.proj-header { 
			display: flex; 
			justify-content: space-between; 
			align-items: flex-end; 
			gap: 2rem; 
			flex-wrap: wrap; 
			margin-bottom: 2rem; 
		}
		.filters { 
			display: flex; 
			gap: 0.5rem; 
			flex-wrap: wrap; 
			margin-bottom: 3rem; 
		}
		.filter-btn {
			padding: 0.38rem 1rem; 
			border-radius: 8px; 
			border: 1px solid var(--border);
			background: var(--surface); 
			color: var(--muted);
			font-family: 'JetBrains Mono', monospace; 
			font-size: 0.78rem;
			cursor: pointer; 
			transition: all var(--t);
		}
		.filter-btn:hover { 
			border-color: var(--border2); 
			color: var(--accent); 
		}
		.filter-btn.active { 
			background: var(--accent);
			border-color: var(--accent); 
			color: var(--bg); 
			font-weight: 600; 
		}

		.proj-grid { 
			display: grid; 
			grid-template-columns: repeat(3,1fr); 
			gap: 1.5rem; 
		}
		.proj-card {
			background: var(--surface); 
			border: 1px solid var(--border);
			border-radius: 16px; 
			overflow: hidden; 
			transition: all var(--t);
		}
		.proj-card:hover { 
			border-color: var(--border2); 
			transform: translateY(-5px); 
			box-shadow: 0 20px 60px rgba(0,0,0,0.5); 
		}
		.proj-card.hidden { 
			display: none; 
		}

		.proj-img-wrap { 
			overflow: hidden; 
			position: relative; 
		}
		.proj-img { 
			width: 100%; 
			height: 190px; 
			object-fit: cover; 
			transition: transform 0.5s ease; 
			filter: brightness(0.82) saturate(0.7); 
		}
		.proj-card:hover .proj-img { 
			transform: scale(1.07); 
			filter: brightness(1) saturate(1); 
		}
		.proj-img-ov { position: absolute; 
			inset: 0; 
			background: linear-gradient(to top, var(--surface) 0%, transparent 55%); 
		}

		.proj-body { 
			padding: 1.4rem; 
		}
		.proj-tags { display: flex; 
			gap: 0.4rem; 
			flex-wrap: wrap; 
			margin-bottom: 0.8rem; 
		}
		.proj-tag {
			font-family: 'JetBrains Mono', monospace; 
			font-size: 0.68rem;
			padding: 0.18rem 0.5rem; 
			border-radius: 5px; 
			background: var(--bg3);
			border: 1px solid var(--border); 
			color: var(--accent2);
		}
		.proj-title { 
			font-size: 1.05rem; 
			font-weight: 700; 
			margin-bottom: 0.5rem; 
		}
		.proj-desc { font-size: 0.87rem; 
			color: var(--muted); 
			line-height: 1.6; 
			margin-bottom: 1.2rem; 
		}
		.proj-links { 
			display: flex; 
			gap: 0.8rem; 
		}
		.plink {
			display: inline-flex; 
			align-items: center; 
			gap: 0.35rem;
			font-family: 'JetBrains Mono', monospace; 
			font-size: 0.8rem; 
			font-weight: 600;
			transition: all var(--t);
		}
		.plink-live { 
			color: var(--accent); 
		}
		.plink-live:hover { 
			text-shadow: 0 0 10px rgba(99,179,237,0.4); 
		}
		.plink-repo { 
			color: var(--muted);
		}
		.plink-repo:hover { 
			color: var(--text); 
		}

		/* CONTACTO */
		#contacto { 
			background: var(--bg2); 
		}
		.contact-grid { 
			display: grid; 
			grid-template-columns: 1fr 1.3fr; 
			gap: 5rem; 
			align-items: start; 
			margin-top: 4rem; 
		}
		.contact-info > p { 
			color: var(--muted); 
			margin-bottom: 2.5rem; 
			line-height: 1.85; 
		}
		.contact-items { 
			display: flex; 
			flex-direction: column; 
			gap: 1rem;
		}
		.contact-item {
			display: flex; 
			align-items: center; 
			gap: 1rem; 
			padding: 1rem 1.2rem;
			border-radius: 12px; 
			border: 1px solid var(--border); 
			background: var(--surface);
			transition: all var(--t);
		}
		.contact-item:hover { 
			border-color: var(--border2); 
			transform: translateX(4px); 
		}
		.ci-icon {
			width: 38px; 
			height: 38px; 
			border-radius: 10px; 
			background: var(--glow);
			border: 1px solid var(--border2); 
			display: flex; 
			align-items: center; 
			justify-content: center;
			color: var(--accent); 
			flex-shrink: 0;
		}
		.ci-icon svg { 
			width: 16px; 
			height: 16px; 
		}
		.ci-lbl { 
			font-family: 'JetBrains Mono', monospace; 
			font-size: 0.7rem; 
			color: var(--muted); 
		}
		.ci-val { 
			font-size: 0.9rem; 
			font-weight: 600; 
		}

		.cform { 
			display: flex; 
			flex-direction: column; 
			gap: 1.2rem; 
		}
		.form-row { 
			display: grid; 
			grid-template-columns: 1fr 1fr; 
			gap: 1.2rem; 
		}
		.form-group { 
			display: flex; 
			flex-direction: column; 
			gap: 0.4rem; 
		}
		label { 
			font-family: 'JetBrains Mono', monospace; 
			font-size: 0.72rem; 
			color: var(--accent); 
			letter-spacing: 0.05em; 
		}
		input, textarea {
			padding: 0.85rem 1rem; 
			border-radius: 10px; 
			border: 1px solid var(--border);
			background: var(--surface); 
			color: var(--text);
			font-family: 'Outfit', sans-serif; 
			font-size: 0.95rem; 
			outline: none;
			transition: border-color var(--t), box-shadow var(--t);
		}
		input:focus, textarea:focus { 
			border-color: var(--border2); 
			box-shadow: 0 0 0 3px var(--glow); 
		}
		textarea { 
			resize: vertical; 
			min-height: 130px; 
		}
		::placeholder { 
			color: var(--muted); 
			opacity: 0.55; 
		}

		.btn-send {
			padding: 0.9rem 2rem; 
			background: var(--accent); 
			color: var(--bg);
			border: none; 
			border-radius: 10px; 
			font-family: 'Outfit', sans-serif;
			font-size: 1rem; 
			font-weight: 700; 
			cursor: pointer;
			display: flex; 
			align-items: center; 
			justify-content: center; 
			gap: 0.5rem;
			transition: all var(--t);
		}
		.btn-send:hover { 
			transform: translateY(-2px); 
			box-shadow: 0 8px 30px rgba(99,179,237,0.3); 
		}

		.form-ok {
			display: none; 
			text-align: center; 
			padding: 3rem 2rem;
			background: var(--surface); 
			border: 1px solid var(--border2); 
			border-radius: 16px;
		}
		.form-ok.show { 
			display: block; 
		}
		.form-ok-icon { 
			font-size: 2.5rem; 
			margin-bottom: 1rem; 
		}
		.form-ok h3 { 
			font-family: 'Bebas Neue', sans-serif; 
			font-size: 1.8rem; 
			letter-spacing: 2px; 
			margin-bottom: 0.5rem; 
			color: var(--accent); 
		}
		.form-ok p { 
			color: var(--muted); 
		}

		/* FOOTER */
		footer { 
			background: var(--bg); 
			border-top: 1px solid var(--border); 
			padding: 2.5rem 2rem; 
			position: relative; 
			z-index: 1; 
		}
		.footer-inner { 
			max-width: 1100px; 
			margin: 0 auto; 
			display: flex; 
			justify-content: space-between; 
			align-items: center; 
			flex-wrap: wrap; 
			gap: 1.5rem; 
		}
		.footer-logo { 
			font-family: 'Bebas Neue', sans-serif; 
			font-size: 1.3rem; 
			letter-spacing: 3px; 
			color: var(--accent); 
		}
		.footer-logo span { 
			color: var(--text); 
		}
		.footer-copy { 
			font-family: 'JetBrains Mono', monospace; 
			font-size: 0.72rem; 
			color: var(--muted); 
			margin-top: 0.3rem; 
		}
		.socials { 
			display: flex; 
			gap: 0.6rem; 
		}
		.social {
			width: 38px; 
			height: 38px; 
			border-radius: 9px; 
			border: 1px solid var(--border);
			background: var(--surface); 
			display: flex; 
			align-items: center; 
			justify-content: center;
			color: var(--muted); 
			transition: all var(--t);
		}
		.social:hover { 
			border-color: var(--border2); 
			color: var(--accent); 
			transform: translateY(-2px); 
		}
		.social svg { 
			width: 16px; 
			height: 16px; 
		}

		/* ANIMATIONS */
		@keyframes fadeUp {
			from { 
				opacity: 0; 
				transform: translateY(24px); 
			}
			to   { 
				opacity: 1; 
				transform: translateY(0); 
			}
		}
		.reveal { 
			opacity: 0; 
			transform: translateY(28px); 
			transition: opacity 0.7s ease, transform 0.7s ease; 
		}
		.reveal.visible { 
			opacity: 1; 
			transform: none; 
		}

		/* RESPONSIVE */
		@media (max-width: 1024px) {
			.proj-grid { 
				grid-template-columns: repeat(2,1fr); 
			}
			.about-grid { 
				grid-template-columns: 1fr; 
				gap: 3rem; 
			}
			.about-img-wrap { 
				max-width: 360px; 
			}
			.contact-grid { 
				grid-template-columns: 1fr; 
				gap: 3rem; 
			}
		}
		@media (max-width: 768px) {
			nav, .btn-contact { 
				display: none; 
			}
			.hamburger { 
				display: flex; 
			}
			.proj-grid { 
				grid-template-columns: 1fr; 
			}
			section { 
				padding: 5rem 1.5rem; 
			}
			.hero-stats { 
				gap: 2rem; 
			}
			.form-row { 
				grid-template-columns: 1fr; 
			}
		}
	</style>

	<script>
		document.getElementById('yr').textContent = String(new Date().getFullYear());

		// Scroll progress
		const prog = document.getElementById('prog');
		window.addEventListener('scroll', () => {
			prog.style.width = (window.scrollY / (document.body.scrollHeight - window.innerHeight) * 100) + '%';
		}, { passive: true });

		// Hamburger
		const ham = document.getElementById('ham');
		const mNav = document.getElementById('mobileNav');
		ham.addEventListener('click', () => { ham.classList.toggle('open'); mNav.classList.toggle('open'); });
			mNav.querySelectorAll('a').forEach(a => a.addEventListener('click', () => {
			ham.classList.remove('open'); mNav.classList.remove('open');
		}));

		// Reveal on scroll
		const ro = new IntersectionObserver((entries) => {
		entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); ro.unobserve(e.target); } });
		}, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });
		document.querySelectorAll('.reveal').forEach(el => ro.observe(el));

		// Skill bars
		const so = new IntersectionObserver((entries) => {
			entries.forEach(e => {
				if (e.isIntersecting) {
				e.target.querySelectorAll('.skill-fill').forEach(f => {
					f.style.transform = `scaleX(${getComputedStyle(f).getPropertyValue('--w').trim()})`;
					f.classList.add('on');
				});
				so.unobserve(e.target);
				}
			});
		}, { threshold: 0.2 });
		const sl = document.querySelector('.skill-list');
		if (sl) so.observe(sl);

		// Filters
		document.querySelectorAll('.filter-btn').forEach(btn => {
			btn.addEventListener('click', () => {
				document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
				btn.classList.add('active');
				const f = btn.dataset.f;
				document.querySelectorAll('.proj-card').forEach(c => {
				c.classList.toggle('hidden', f !== 'all' && c.dataset.f !== f);
				});
			});
		});

		// Form
		const cform = document.getElementById('cform');
		const formOk = document.getElementById('formOk');
		cform.addEventListener('submit', async (e) => {
			e.preventDefault();
			const btn = cform.querySelector('.btn-send');
			btn.textContent = 'Enviando...';
			btn.disabled = true;
			try {
				const res = await fetch(cform.action, { method: 'POST', body: new FormData(cform), headers: { 'Accept': 'application/json' } });
				if (res.ok) { cform.style.display = 'none'; formOk.classList.add('show'); }
				else { btn.textContent = 'Error, intentá de nuevo'; btn.disabled = false; }
			} catch { btn.textContent = 'Error de conexión'; btn.disabled = false; }
		});
	</script>

</Layout>